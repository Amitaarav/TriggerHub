# ──────────────────────────────────────────────────
# This directory holds Docker secret files for production.
# NEVER commit actual secret values to git.
#
# Setup:
#   echo "your-strong-db-password"         > db_password.txt
#   echo "your-32-char-jwt-secret"         > jwt_secret.txt
#   echo "your-32-char-jwt-refresh-secret" > jwt_refresh_secret.txt
#   echo "your-32-char-aadhar-salt"        > aadhar_salt.txt
# ──────────────────────────────────────────────────
